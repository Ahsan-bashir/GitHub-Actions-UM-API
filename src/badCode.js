// This file has intentional linting errors
const unusedVariable = "test"  // Missing semicolon, unused variable

function badFunction( ){  // Bad spacing
var oldStyle = 'use const'  // Use const instead of var
  if(true){console.log("bad formatting")}  // Bad formatting
}

module.exports=badFunction  // Missing semicolon